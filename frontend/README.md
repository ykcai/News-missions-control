# Frontend (React) — Mission Control HUD

## Prereqs
- Node.js LTS (18+ or 20+ recommended)

## Setup
```powershell
cd frontend
npm install
```

## Run (dev)
```powershell
npm run dev
```

Open: http://localhost:5173

> The dev server proxies `/api` to the Python backend at `http://localhost:8000`.
